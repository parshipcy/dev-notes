# SQL

Notes and runnable examples for learning SQL basics.

Everything lives in [`main.sql`](./main.sql): queries to run, with comments explaining each concept along the way. Use MySQL Workbench (or any MySQL client) to execute them.
